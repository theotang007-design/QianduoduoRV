# A股智能复盘系统 —— 需求文档

## 1. 项目背景与目标

个人投资者持有若干A股股票，希望每天收盘后自动获取：
1. 持仓股票当天的相关消息（新闻、公告），并由AI判断相关性与利好/利空倾向；
2. 持仓股票当天的技术面数据（价格、成交量、常用技术指标）；
3. 一份结构化的复盘报告，通过本地Web页面查看，并保留历史记录可回溯查询。

**系统定位：信息整理与辅助分析工具，不做买卖决策，不构成投资建议。** 这一点需要在产品设计和界面上明确体现（见第7节）。

**部署环境**：Windows个人电脑，本地运行，不涉及公网部署、不需要多用户支持。

---

## 2. 总体架构

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐     ┌────────────┐
│  数据采集层  │ --> │  AI分析层    │ --> │  存储层     │ --> │  Web展示层 │
│ (行情/新闻/  │     │ (相关性判断/ │     │ (SQLite)    │     │ (本地网页) │
│  公告抓取)   │     │  情绪打分)   │     │             │     │            │
└─────────────┘     └──────────────┘     └─────────────┘     └────────────┘
        ↑
   定时任务调度（Windows任务计划程序，每日收盘后触发）
```

### 技术选型建议（可由开发方按实际情况调整）
| 模块 | 建议方案 | 说明 |
|---|---|---|
| 行情/公告数据 | `akshare`（Python开源库） | 免费，覆盖A股行情、K线、公告 |
| 新闻抓取 | 东方财富个股新闻、财联社电报等公开页面 | 按关键词（股票名称+代码）定向抓取 |
| AI分析 | 火山引擎 API / DeepSeek API | 二选一或都接入，做新闻相关性与情绪判断 |
| 数据存储 | SQLite | 本地文件数据库，免安装，支持历史查询 |
| Web服务 | Flask 或 FastAPI | 本地轻量web服务，`localhost`访问 |
| 定时调度 | Windows 任务计划程序 | 每日收盘后（建议15:30）自动触发抓取+分析流程 |

---

## 3. 功能需求详细拆解

### 3.1 持仓管理
- 用户可维护一份持仓列表：股票代码、股票名称、（可选）持仓成本价、持仓数量
- 存储位置：数据库表 + 提供一个简单的配置文件/网页表单供用户增删改（不要求用户手动改代码或数据库）
- 用户改动持仓后，次日自动生效，无需重启服务

### 3.2 数据采集模块
**行情数据**（每只持仓股票，每个交易日收盘后）：
- 当日开盘价、收盘价、最高价、最低价、成交量、成交额、涨跌幅
- 近期K线数据（建议保留最近60个交易日，用于技术面计算）
- 常用技术指标：MA5/MA10/MA20均线、MACD、RSI、成交量变化率

**新闻/公告数据**（每只持仓股票，每个交易日）：
- 按"股票名称"+"股票代码"关键词，抓取当日相关新闻标题、正文摘要、来源、发布时间、原文链接
- 抓取交易所公告（如有）
- 去重处理：同一事件被多家媒体转载时，只保留一条代表性新闻，避免复盘报告冗余

**异常处理要求**：
- 某只股票当天抓不到新闻时，不报错中断，正常跳过，报告中显示"今日无重大消息"
- 数据源访问失败时需要有重试机制（建议重试2-3次），并在日志中记录失败原因

### 3.3 AI分析模块
对每条抓取到的新闻，调用大模型API，输出结构化结果：

```json
{
  "股票代码": "600519",
  "新闻标题": "...",
  "相关性": "高/中/低/无关",
  "情绪倾向": "利好/利空/中性",
  "简要理由": "一句话说明AI判断依据",
  "事件分类": "业绩/政策/行业/公司治理/突发事件/其他"
}
```

**要求**：
- 相关性判断为"无关"的新闻，不进入最终报告（过滤噪音）
- 每条判断需保留"简要理由"，让用户能理解AI为什么这样判断，而不是只给一个标签
- Prompt设计中需要明确要求AI**只做信息层面的解读，不生成"建议买入/卖出"之类的措辞**，避免报告变成变相的投资建议
- 调用失败（超时、限流等）时需要有重试和降级逻辑，不能导致整个复盘流程中断

### 3.4 技术面简报模块
基于3.2采集的行情数据，生成简要技术面描述，例如：
- 当日涨跌幅及近5日/20日趋势方向
- 是否触及关键均线（如跌破MA20、站上MA5等）
- 成交量较近期均值的变化（放量/缩量）
- 这部分为**客观数据陈述**，不做买卖信号判断

### 3.5 复盘报告生成
每个交易日收盘后自动生成一份报告，结构建议：

```
【每日复盘报告】2026-08-10

一、持仓总览
   - 股票列表 + 当日涨跌幅一览

二、逐股详情（按持仓股票循环）
   ┌ 股票名称（代码）
   ├ 技术面：今日涨跌幅 / 量能变化 / 均线位置
   ├ 消息面：
   │   [利好] 新闻标题 —— 简要理由（来源，发布时间，原文链接）
   │   [利空] 新闻标题 —— 简要理由
   │   [中性] 新闻标题 —— 简要理由
   └ （无相关新闻则显示"今日无重大消息"）

三、风险提示（固定文案，见第7节）
```

### 3.6 历史记录与查询
- 所有报告、原始新闻、AI分析结果均需持久化存储，永久保留（除非用户手动清理）
- Web界面提供按日期查询历史报告的功能（如日历选择器或列表翻页）
- 提供按个股查询历史消息的功能（例如查看某只股票近1个月的所有相关消息）

### 3.7 Web展示层
- 本地Web服务，浏览器访问 `http://localhost:端口号`
- **首页**：今日复盘报告（默认展示最新一期）
- **历史页**：按日期浏览历史报告
- **个股详情页**：点击某只股票，查看该股历史消息+技术指标走势图（K线图可用开源图表库如ECharts实现）
- **持仓管理页**：增删改持仓股票

---

## 4. 数据模型建议（表结构）

| 表名 | 主要字段 | 说明 |
|---|---|---|
| `holdings` | 股票代码, 股票名称, 持仓数量, 成本价, 添加时间 | 持仓管理 |
| `daily_quotes` | 股票代码, 日期, 开盘价, 收盘价, 最高价, 最低价, 成交量, 涨跌幅, 均线值等 | 行情历史 |
| `news_raw` | id, 股票代码, 标题, 正文摘要, 来源, 发布时间, 原文链接, 抓取时间 | 原始新闻 |
| `news_analysis` | news_id(关联news_raw), 相关性, 情绪倾向, 简要理由, 事件分类 | AI分析结果 |
| `daily_reports` | 日期, 报告内容(可存为结构化JSON或渲染好的HTML), 生成时间 | 复盘报告归档 |

---

## 5. 定时任务与自动化

- 使用Windows任务计划程序，设定每个交易日15:30后触发一次完整流程：采集 → AI分析 → 生成报告 → 存库
- 需判断当日是否为交易日（可用akshare自带的交易日历接口），非交易日自动跳过
- 整个流程应可通过一条命令/一个脚本手动触发（方便用户临时补跑或调试）

---

## 6. 非功能需求

- **成本控制**：AI API调用需注意控制新闻数量（如去重、限制每股票每日最多分析N条新闻），避免API费用失控
- **容错**：任一环节（抓取失败、API超时）不应导致整个复盘流程崩溃，需有日志记录，方便排查
- **数据安全**：API Key等敏感信息通过本地配置文件（如`.env`）管理，不写死在代码中，不上传到任何公开仓库
- **可维护性**：持仓股票的增删、抓取的新闻源，应尽量做成可配置项，而不是写死在代码逻辑里

---

## 7. 重要产品要求：风险提示与边界

系统生成的所有报告，页面底部或报告末尾需固定展示以下文案（内容可调整措辞，但含义需保留）：

> 本报告由程序自动抓取公开信息并结合AI辅助分析生成，仅供信息整理参考，可能存在信息遗漏、延迟或AI误判，不构成任何投资建议。股市有风险，投资决策及后果需由使用者自行判断和承担。

同时要求：
- AI分析的Prompt中不得出现"建议买入""建议卖出""目标价"等表述
- 报告界面不使用"买入信号""卖出信号"等按钮化、指令化的呈现方式，只做信息陈述

---

## 8. 分期开发建议

**第一期（MVP，建议优先跑通）**：
- 1-2只股票的完整链路：行情抓取 → 新闻抓取 → AI分析 → 存库 → 网页展示（无需历史查询、无需图表）
- 验证AI分析质量和整体流程稳定性

**第二期**：
- 扩展到全部持仓股票（6-15只）
- 增加K线图表、历史查询、个股详情页
- 增加定时任务自动化
- 优化报告排版和Prompt效果

---

## 9. 验收标准（供开发自测参考）

- [ ] 收盘后自动运行，无需人工干预即可生成当日报告
- [ ] 任意一只股票当日无新闻时，流程不报错，报告正常显示"无重大消息"
- [ ] Web页面可正常查看当日报告及至少30天的历史报告
- [ ] AI分析结果中不包含"买入/卖出"类建议措辞
- [ ] 报告中每条消息均可追溯到原文链接
- [ ] 系统重启或电脑重启后，历史数据不丢失
